import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ChannelsTreeComponent } from './channels-tree.component';

describe('ChannelsTreeComponent', () => {
 /* let component: ChannelsTreeComponent;
  let fixture: ComponentFixture<ChannelsTreeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ChannelsTreeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ChannelsTreeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
